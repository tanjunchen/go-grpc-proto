syntax = "proto3";

package services;

message ProdRequest {
  // 商品ID
  int32 prod_id = 1;
}

message ProdResponse {
  // 商品库存
  int32 prod_stock = 1;
}

service ProductService {
  rpc  GetProductStock (ProdRequest) returns (ProdResponse);
}